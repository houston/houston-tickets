td.ticket-type { padding: 0 !important; }
.ticket-type {
  background: #efefef;
  width: 6px;
  min-width: 6px;
  max-width: 6px;

  &.ticket-type-na { background: transparent; }
  <% Houston::Tickets.config.ticket_colors.each do |name, value| %>
    &.ticket-type-<%= name.downcase %> { background: #<%= value %> !important; }
  <% end %>
}
